<%= render 'shared_pages/page_title', title: "Page data in charts" %>

<div class="container">
    <%= pie_chart Chef.group(:chef_stars).count %>
</div> 

<div class="container">
    <%= line_chart Recipe.group(:created_at).count %>
</div> 
<div class="container">
    <%= column_chart Recipe.group(:name).count %>
</div>


<div class="container">
    <%=  column_chart Recipe.group(:chefname).group(:cooking_time).count, legend: true %>
</div>

<div class="container">
    <%=  column_chart Chef.group(:chefname).group(:name).count %>

</div>
<div class="container">
    <%=  column_chart Recipe.group(:title).group(:name).count %>

</div>

